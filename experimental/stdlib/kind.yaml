# Kind Controller
#
# The bootstrap root. A Graph that implements the Kind type. Creates the
# Kind CRD, watches all Kinds, creates a CRD per Kind, watches instances
# of each Kind, and creates per-instance Graphs.
#
# Convention: inside a Kind's per-instance Graph, `schema` is the scope
# variable (the resource itself). User nodes reference ${schema.spec.foo}.
---
apiVersion: experimental.kro.run/v1alpha1
kind: Graph
metadata:
  name: kind
  namespace: kro-system
spec:
  nodes:
    - id: kindCrd
      template:
        apiVersion: apiextensions.k8s.io/v1
        kind: CustomResourceDefinition
        metadata:
          name: kinds.experimental.kro.run
        spec:
          group: experimental.kro.run
          names:
            plural: kinds
            singular: kind
            kind: Kind
            listKind: KindList
          scope: Cluster
          versions:
            - name: v1alpha1
              served: true
              storage: true
              schema:
                openAPIV3Schema: >-
                  ${simpleSchema.toOpenAPI({"types": {"Node": {"id": "string", "template": "any", "patch": "any", "ref": "any", "watch": "any", "def": "any", "forEach": "[]map[string]string", "finalizes": "string", "includeWhen": "[]string", "readyWhen": "[]string", "propagateWhen": "[]string"}, "PrinterColumn": {"name": "string | required=true", "type": "string | required=true", "jsonPath": "string | required=true", "description": "string", "format": "string", "priority": "integer"}}, "spec": {"scope": "string | default=Namespaced", "schema": {"apiVersion": "string | required=true pattern=^[a-z][a-z0-9.-]*/[a-z][a-z0-9]*$", "kind": "string | required=true", "spec": "object", "status": "object", "types": "object"}, "propagateWhen": "[]string", "nodes": "[]Node", "additionalPrinterColumns": "[]PrinterColumn"}, "status": {"ready": "boolean", "items": "integer"}}, [])}
              subresources:
                status: {}
              additionalPrinterColumns:
                - name: Kind
                  type: string
                  jsonPath: .spec.schema.kind
                - name: APIVersion
                  type: string
                  jsonPath: .spec.schema.apiVersion
                - name: Scope
                  type: string
                  jsonPath: .spec.scope
                - name: Ready
                  type: boolean
                  jsonPath: .status.ready
                - name: Items
                  type: integer
                  jsonPath: .status.items
                - name: Age
                  type: date
                  jsonPath: .metadata.creationTimestamp

    - id: watchKinds
      includeWhen:
        - ${has(kindCrd.status) && has(kindCrd.status.conditions) && kindCrd.status.conditions.exists(c, c.type == 'Established' && c.status == 'True')}
      # No metadata.namespace: watch Kinds across all namespaces
      # (k8s list/watch semantics — absent namespace = all).
      watch:
        apiVersion: experimental.kro.run/v1alpha1
        kind: Kind
        selector: {}

    - id: controllers
      forEach:
        - k: ${watchKinds}
      template:
        apiVersion: experimental.kro.run/v1alpha1
        kind: Graph
        metadata:
          name: kind-${k.metadata.name}
          namespace: kro-system
        spec:
          nodes:
            - id: k
              ref:
                apiVersion: experimental.kro.run/v1alpha1
                kind: Kind
                metadata:
                  name: ${k.metadata.name}

            - id: crd
              template:
                apiVersion: apiextensions.k8s.io/v1
                kind: CustomResourceDefinition
                metadata:
                  name: $${plural(k.spec.schema.kind).lowerAscii()}.$${k.spec.schema.apiVersion.split('/')[0]}
                spec:
                  group: $${k.spec.schema.apiVersion.split('/')[0]}
                  names:
                    kind: $${k.spec.schema.kind}
                    plural: $${plural(k.spec.schema.kind).lowerAscii()}
                  scope: $${k.spec.scope}
                  versions:
                    - name: $${k.spec.schema.apiVersion.split('/')[1]}
                      served: true
                      storage: true
                      subresources:
                        status: {}
                      schema:
                        openAPIV3Schema: $${simpleSchema.toOpenAPI(k.spec.schema, k.spec.nodes)}
                      additionalPrinterColumns: '$${has(k.spec.additionalPrinterColumns) ? k.spec.additionalPrinterColumns : []}'

            - id: watchInstances
              includeWhen:
                - $${has(crd.status) && has(crd.status.conditions) && crd.status.conditions.exists(c, c.type == 'Established' && c.status == 'True')}
              # No metadata.namespace: watch items of this Kind
              # across all namespaces (k8s list/watch semantics —
              # absent namespace = all).
              watch:
                apiVersion: $${k.spec.schema.apiVersion}
                kind: $${k.spec.schema.kind}
                metadata:
                  selector: {}

            - id: instances
              forEach:
                - instance: $${watchInstances}
              readyWhen:
                - >-
                  $${instances.status.conditions.exists(c,
                  c.type == 'Ready' && c.status == 'True')}
              propagateWhen: "${has(k.spec.propagateWhen) ? k.spec.propagateWhen : []}"
              template:
                apiVersion: experimental.kro.run/v1alpha1
                kind: Graph
                metadata:
                  name: >-
                    $${(has(instance.metadata.namespace) && instance.metadata.namespace != ""
                    ? instance.metadata.namespace + "-" : "")
                    + instance.metadata.name + "-" + k.spec.schema.kind.lowerAscii()}
                spec:
                  nodes: >-
                    $${
                      [{"id": "schema", "ref": {
                        "apiVersion": k.spec.schema.apiVersion,
                        "kind": k.spec.schema.kind,
                        "metadata": {
                          "name": instance.metadata.name,
                          "namespace": has(instance.metadata.namespace) ? instance.metadata.namespace : ""
                        }
                      }}]
                      + k.spec.nodes
                    }

            - id: kindStatus
              patch:
                apiVersion: experimental.kro.run/v1alpha1
                kind: Kind
                metadata:
                  name: $${k.metadata.name}
                status:
                  ready: true
                  items: $${watchInstances.size()}

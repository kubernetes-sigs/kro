apiVersion: kro.run/v1alpha1
kind: ResourceGraphDefinition
metadata:
  name: upgrade-includewhen-empty
spec:
  schema:
    apiVersion: v1alpha1
    kind: UpgradeIncludeEmpty
    spec:
      name: string
    status: {}
  resources:
  - id: configmap
    includeWhen: []
    template:
      apiVersion: v1
      kind: ConfigMap
      metadata:
        name: ${schema.spec.name}-configmap
        namespace: ${schema.metadata.namespace}
      data:
        test: includewhen-empty

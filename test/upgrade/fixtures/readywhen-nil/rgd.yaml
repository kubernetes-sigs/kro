apiVersion: kro.run/v1alpha1
kind: ResourceGraphDefinition
metadata:
  name: upgrade-readywhen-nil
spec:
  schema:
    apiVersion: v1alpha1
    kind: UpgradeReadyNil
    spec:
      name: string
    status: {}
  resources:
  - id: configmap
    # readyWhen intentionally omitted (nil)
    template:
      apiVersion: v1
      kind: ConfigMap
      metadata:
        name: ${schema.spec.name}-configmap
        namespace: ${schema.metadata.namespace}
      data:
        test: readywhen-nil
